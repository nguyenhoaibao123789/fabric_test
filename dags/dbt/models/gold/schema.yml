version: 2

models:
  - name: dim_carrier
    description: Carrier dimension — one row per carrier code.
    columns:
      - name: carrier_code
        description: Natural key — carrier identifier (e.g. FEDEX, DHL)
        tests:
          - unique
          - not_null
      - name: is_active
        tests:
          - not_null
